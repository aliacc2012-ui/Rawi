import{NextResponse}from"next/server";
export async function POST(request:Request){try{const payload=await request.json();const safe={type:String(payload?.type??"client-error").slice(0,80),message:String(payload?.message??"Unknown client error").slice(0,2000),stack:typeof payload?.stack==="string"?payload.stack.slice(0,8000):undefined,path:typeof payload?.path==="string"?payload.path.slice(0,500):undefined,source:typeof payload?.source==="string"?payload.source.slice(0,500):undefined,line:Number.isFinite(payload?.line)?payload.line:undefined,column:Number.isFinite(payload?.column)?payload.column:undefined,ts:typeof payload?.ts==="string"?payload.ts:new Date().toISOString(),userAgent:typeof payload?.userAgent==="string"?payload.userAgent.slice(0,500):undefined};console.error("[RAWI_CLIENT_ERROR]",JSON.stringify(safe));return NextResponse.json({ok:true})}catch{return NextResponse.json({ok:false},{status:400})}}
