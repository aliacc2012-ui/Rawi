import Link from "next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{getCurrentWorkspace}from"@/lib/workspace";import{HomeSocialLinks}from"@/components/app-shell/HomeSocialLinks";
type DashboardStats={project_count:number;photo_count:number;published_count:number;download_count:number;favorite_count:number;new_feedback_count:number;feedback_by_project:Record<string,number>};
const EMPTY:DashboardStats={project_count:0,photo_count:0,published_count:0,download_count:0,favorite_count:0,new_feedback_count:0,feedback_by_project:{}};
export default async function DashboardPage(){const{profile,workspace}=await getCurrentWorkspace();if(!workspace)redirect("/onboarding");const s=await createClient();const[{data:projects},{data:statsData,error:statsError}]=await Promise.all([s.from("projects").select("id,name,status,project_type,created_at").eq("workspace_id",workspace.id).order("created_at",{ascending:false}).limit(5),s.rpc("rawi_dashboard_stats",{target_workspace_id:workspace.id})]);const recentProjects=projects??[];const stats=!statsError&&statsData?statsData as DashboardStats:EMPTY;const feedbackByProject=stats.feedback_by_project??{};const links={instagram:workspace.instagram_url||"",tiktok:workspace.tiktok_url||"",facebook:workspace.facebook_url||"",website:workspace.website_url||""};const recent=recentProjects[0];const recentFeedback=recent?feedbackByProject[recent.id]??0:0;const feedbackProject=recentProjects.find(p=>(feedbackByProject[p.id]??0)>0);return <div className="max-w-[1500px] mx-auto pb-8"><header className="flex flex-col xl:flex-row xl:items-start justify-between gap-6 mb-7"><div><span className="text-[11px] font-extrabold tracking-[.18em] text-gray-400">CREATOR WORKSPACE</span><h1 className="text-[34px] md:text-[42px] tracking-[-.05em] mt-3">Good morning, {profile?.full_name?.split(" ")[0]||"there"}.</h1><p className="text-xl text-gray-400 mt-2">Ready to deliver something beautiful?</p><div className="flex gap-3 mt-6"><Link href="/projects" prefetch className="bg-rawi-yellow text-black rounded-xl px-5 py-3 font-extrabold text-sm">＋ New Project</Link><Link href="/projects" prefetch className="bg-white border rounded-xl px-5 py-3 font-bold text-sm">↥ Upload Media</Link></div></div><HomeSocialLinks links={links}/></header><section className="grid grid-cols-2 md:grid-cols-4 xl:grid-cols-7 gap-3 mb-5"><Stat value={String(stats.project_count)} label="Projects"/><Stat value={String(stats.photo_count)} label="Photos"/><Stat value={String(stats.published_count)} label="Galleries"/><Stat value={String(stats.download_count)} label="Downloads"/><Stat value={String(stats.favorite_count)} label="Favorites"/>{feedbackProject?<Link href={`/projects/${feedbackProject.id}#feedback`} prefetch className="block"><Stat value={String(stats.new_feedback_count)} label="New feedback" alert/></Link>:<Stat value={String(stats.new_feedback_count)} label="New feedback" alert={stats.new_feedback_count>0}/>}<Stat value={`${((workspace.storage_used_bytes||0)/1024**3).toFixed(1)} GB`} label="Storage used"/></section><section className="grid grid-cols-1 xl:grid-cols-[1.12fr_.88fr] gap-5"><div className="bg-white border rounded-[22px] p-5"><h2 className="text-xl font-bold mb-4">Recent Projects</h2>{recent?<div className="relative rounded-2xl bg-[#111] text-white p-8 min-h-[330px] flex flex-col justify-end">{recentFeedback>0&&<Link href={`/projects/${recent.id}#feedback`} prefetch className="absolute right-5 top-5 rounded-full bg-rawi-yellow px-3 py-2 text-xs font-extrabold text-black hover:scale-105 transition">💬 {recentFeedback} new feedback</Link>}<div className="text-4xl">{recent.name}</div><p className="text-white/50 mt-2 capitalize">{recent.project_type.replace("_"," ")} · {recent.status}</p><Link href={`/projects/${recent.id}`} prefetch className="mt-5 bg-rawi-yellow text-black rounded-full px-5 py-3 font-bold text-sm self-start">Open project</Link></div>:<div className="rounded-2xl bg-[#111] text-white p-10 min-h-[320px] flex flex-col justify-end"><h3 className="text-3xl">Create a project worth sharing.</h3></div>}</div><div className="bg-white border rounded-[22px] p-5"><h2 className="text-xl font-bold mb-5">Activity</h2>{feedbackProject&&<Link href={`/projects/${feedbackProject.id}#feedback`} prefetch><Activity title={`${stats.new_feedback_count} new client feedback`} accent/></Link>}<Activity title={`${stats.favorite_count} client favorites`}/><Activity title={`${stats.download_count} downloads`}/><Activity title={`${stats.published_count} published galleries`}/></div></section></div>}
function Stat({value,label,alert=false}:{value:string;label:string;alert?:boolean}){return <div className={`bg-white border rounded-[20px] p-4 min-h-[120px] ${alert?"border-amber-200 hover:shadow-md transition":""}`}><div className={`text-2xl font-extrabold mt-8 ${alert?"text-amber-600":""}`}>{value}</div><div className="text-xs font-bold mt-1">{label}</div></div>}function Activity({title,accent=false}:{title:string;accent?:boolean}){return <div className={`py-3 border-b border-gray-100 text-sm font-bold ${accent?"text-amber-600 hover:text-amber-700":""}`}>{accent&&"💬 "}{title}{accent&&" →"}</div>}
