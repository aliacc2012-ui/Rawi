import { getCurrentWorkspace } from "@/lib/workspace";
import { createClient } from "@/lib/supabase/server";
import { BrandingForm } from "@/components/app-shell/BrandingForm";
import { SocialLinksForm } from "@/components/app-shell/SocialLinksForm";
import { ThumbnailBackfill } from "@/components/app-shell/ThumbnailBackfill";
import { PlanCheckoutButton } from "@/components/billing/PlanCheckoutButton";

export default async function SettingsPage(){const{workspace}=await getCurrentWorkspace();const accent=workspace!.accent_color||"#FFD400";const supabase=await createClient();const{data:social}=await supabase.from("workspaces").select("instagram_url,tiktok_url,facebook_url,website_url").eq("id",workspace!.id).single();const initialLinks={instagram:social?.instagram_url||"",tiktok:social?.tiktok_url||"",facebook:social?.facebook_url||"",website:social?.website_url||""};const{data:projectRows}=await supabase.from("projects").select("id").eq("workspace_id",workspace!.id);const projectIds=(projectRows??[]).map(p=>p.id);let activeGalleries=0,pendingThumbs=0;if(projectIds.length){const[{count:galleries},{count:thumbs}]=await Promise.all([supabase.from("galleries").select("id",{count:"exact",head:true}).in("project_id",projectIds).eq("status","published"),supabase.from("media").select("id",{count:"exact",head:true}).in("project_id",projectIds).eq("media_type","image").is("thumbnail_path",null)]);activeGalleries=galleries??0;pendingThumbs=thumbs??0}const usedBytes=workspace!.storage_used_bytes||0,limitBytes=workspace!.storage_limit_bytes||1,usedGb=usedBytes/1024**3,limitGb=limitBytes/1024**3,storagePct=Math.min(100,(usedBytes/Math.max(1,limitBytes))*100),freeGb=Math.max(0,limitGb-usedGb);return <div className="max-w-[1500px] mx-auto pb-8"><div className="mb-7"><span className="text-[11px] font-extrabold tracking-[0.18em] text-gray-400">CREATOR WORKSPACE</span><h1 className="text-[34px] md:text-[42px] tracking-[-0.05em] leading-none mt-3">Branding</h1><p className="text-gray-400 mt-2">Shape how clients experience your studio before they even see the first photo.</p></div><div className="grid grid-cols-1 xl:grid-cols-[.9fr_1.1fr] gap-5"><div className="space-y-5"><div className="bg-white border border-gray-200 rounded-[22px] p-5 md:p-6 shadow-sm"><h2 className="text-xl font-bold">Studio identity</h2><p className="text-sm text-gray-400 mt-1 mb-6">Update your public studio name and signature accent.</p><BrandingForm workspaceId={workspace!.id} initialName={workspace!.name} initialAccent={accent}/></div><div className="bg-white border border-gray-200 rounded-[22px] p-5 md:p-6 shadow-sm"><h2 className="text-xl font-bold">Social presence</h2><p className="text-sm text-gray-400 mt-1">Your saved profiles are available on every device.</p><SocialLinksForm workspaceId={workspace!.id} initialLinks={initialLinks}/></div><ThumbnailBackfill workspaceId={workspace!.id} initialPending={pendingThumbs}/></div><div className="xl:sticky xl:top-6 self-start"><div className="bg-white border border-gray-200 rounded-[22px] p-4 shadow-sm"><div className="relative overflow-hidden rounded-[18px] min-h-[520px] bg-[radial-gradient(circle_at_68%_52%,rgba(255,212,0,.16),transparent_24%),linear-gradient(145deg,#353535,#080808_60%,#252525)] text-white"><div className="relative z-10 p-6 md:p-8 min-h-[520px] flex flex-col justify-between"><div className="font-extrabold">{workspace!.name.toUpperCase()}</div><div><span className="text-[10px] font-bold" style={{color:accent}}>PHOTOGRAPHY</span><h3 className="text-5xl md:text-6xl tracking-[-.06em] mt-3">Your next client story.</h3><button className="mt-6 rounded-xl px-5 py-3 text-xs font-extrabold text-black" style={{backgroundColor:accent}}>View Gallery ↓</button></div></div></div></div></div></div><section className="mt-6 space-y-5"><div className="bg-white border border-gray-200 rounded-[24px] p-5 md:p-7 shadow-sm"><h2 className="text-[28px] font-bold">Plan & storage</h2><div className="grid grid-cols-1 lg:grid-cols-2 gap-6 mt-7"><div><div className="text-[40px] font-extrabold uppercase">{workspace!.plan}</div><div className="mt-6"><div className="flex justify-between text-xs font-bold"><span>STORAGE USED</span><span>{usedGb.toFixed(1)} GB / {limitGb.toFixed(1)} GB</span></div><div className="h-2.5 rounded-full bg-[#efefed] mt-3"><div className="h-full rounded-full bg-emerald-500" style={{width:`${storagePct}%`}}/></div><div className="text-xs text-gray-500 mt-3">{freeGb.toFixed(1)} GB free · {activeGalleries} published galleries</div></div></div></div></div><div className="grid grid-cols-1 lg:grid-cols-3 gap-4"><PlanCard name="Free" price="0" current={workspace!.plan==="free"} workspaceId={workspace!.id}/><PlanCard name="Creator" price="49" current={workspace!.plan==="creator"} workspaceId={workspace!.id} featured/><PlanCard name="Pro" price="129" current={workspace!.plan==="pro"} workspaceId={workspace!.id}/></div></section></div>}
function PlanCard({name,price,current,workspaceId,featured=false}:{name:"Free"|"Creator"|"Pro";price:string;current:boolean;workspaceId:string;featured?:boolean}){return <div className={`bg-white rounded-[22px] p-6 min-h-[300px] ${featured?"border-2 border-black":"border border-gray-200"}`}><div className="font-extrabold uppercase">{name}</div><div className="text-[48px] mt-5">{price}<span className="text-sm text-gray-500"> AED{price!=="0"?"/mo":""}</span></div>{current?<button disabled className="mt-12 w-full rounded-full border px-5 py-3 text-sm font-extrabold opacity-50">Current plan</button>:name!=="Free"?<PlanCheckoutButton plan={name.toLowerCase() as "creator"|"pro"} workspaceId={workspaceId} featured={featured}/>:null}</div>}
